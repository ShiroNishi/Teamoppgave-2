
const larsStats = {
    sult: 100,
    tørst: 100,
    hygiene: 100,
    komfort: 100,
    humør: 100,
}

const minNum = 0;
const maxNum = 100;

//let gameOver = Object.values(larsStats).includes(minNum);